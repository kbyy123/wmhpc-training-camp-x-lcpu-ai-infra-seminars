# Neovim / LazyVim 常用快捷键速查表

> 适用于 LazyVim 默认配置（整理日期：2026-08-08）。默认 `<leader>` 是空格键，`<localleader>` 是反斜杠 `\`。个人配置、启用的 Extras 与插件可能覆盖或增加按键。

## 先记住这几个符号

| 写法 | 含义 |
| --- | --- |
| `<leader>` | 空格键 `Space` |
| `<localleader>` | 反斜杠 `\` |
| `<C-x>` | `Ctrl+x` |
| `<A-x>` | `Alt+x` |
| `<S-x>` | `Shift+x` |
| `n` / `i` / `v` | 普通 / 插入 / 可视模式 |

- 不知道下一步按什么时，先按 `Space`，等待 which-key 菜单出现。
- 查看当前缓冲区的可用键位：`<leader>?`
- 搜索全部快捷键：`<leader>sk`
- `Esc`：回到普通模式，同时清除搜索高亮。

## 模式与保存退出

| 按键 | 功能 |
| --- | --- |
| `i` / `a` | 在光标前 / 后进入插入模式 |
| `I` / `A` | 在行首 / 行尾进入插入模式 |
| `o` / `O` | 在下方 / 上方新建一行 |
| `v` / `V` / `<C-v>` | 字符 / 行 / 块选择 |
| `Esc` | 返回普通模式 |
| `<C-s>` | 保存文件（多种模式可用） |
| `:w` | 保存 |
| `:q` / `:q!` | 退出 / 强制退出且不保存 |
| `:wq` / `ZZ` | 保存并退出 |
| `<leader>qq` | 退出全部窗口 |
| `u` / `<C-r>` | 撤销 / 重做 |
| `.` | 重复上一次修改 |

## 移动与跳转

| 按键 | 功能 |
| --- | --- |
| `h j k l` | 左、下、上、右 |
| `w` / `b` / `e` | 下个词首 / 上个词首 / 当前词尾 |
| `0` / `^` / `$` | 行首 / 首个非空字符 / 行尾 |
| `gg` / `G` | 文件开头 / 文件末尾 |
| `{` / `}` | 上一个 / 下一个段落 |
| `<C-u>` / `<C-d>` | 向上 / 向下翻半页 |
| `<C-b>` / `<C-f>` | 向上 / 向下翻整页 |
| `zz` / `zt` / `zb` | 当前行置于屏幕中央 / 顶部 / 底部 |
| `%` | 跳转到匹配的括号 |
| `f{字符}` / `F{字符}` | 跳到本行后方 / 前方的指定字符 |
| `t{字符}` / `T{字符}` | 跳到指定字符之前 / 之后 |
| `;` / `,` | 重复 / 反向重复 `f/F/t/T` |
| `s` | Flash 快速跳转；输入目标字符后按提示字母 |
| `<C-o>` / `<C-i>` | 跳转历史后退 / 前进 |
| `m{字母}` / `'{字母}` | 设置标记 / 跳到标记所在行 |

常用的“数字 + 动作”组合：`5j` 向下 5 行，`3w` 向前 3 个单词，`10G` 跳到第 10 行。

## 编辑组合：操作符 + 范围

Vim 的高效组合通常是“操作符 + 移动范围”：`d` 删除、`c` 修改、`y` 复制，后面接 `w`、`$`、`}`、`i(` 等范围。

| 按键 | 功能 |
| --- | --- |
| `x` | 删除光标处字符 |
| `dd` / `D` | 删除整行 / 删除到行尾 |
| `dw` / `d$` | 删除到下个词首 / 行尾 |
| `cc` / `C` | 修改整行 / 修改到行尾 |
| `cw` | 修改到词尾 |
| `yy` / `Y` | 复制整行 |
| `p` / `P` | 在光标后 / 前粘贴 |
| `r{字符}` | 替换一个字符 |
| `J` | 将下一行合并到当前行 |
| `>>` / `<<` | 当前行增加 / 减少缩进 |
| `=` | 格式化所选范围，如 `gg=G` 格式化全文 |
| `~` | 切换字符大小写 |
| `gU{范围}` / `gu{范围}` | 转为大写 / 小写，如 `gUiw` |
| `ciw` / `diw` / `yiw` | 修改 / 删除 / 复制当前单词 |
| `ci"` / `di"` / `yi"` | 修改 / 删除 / 复制双引号内内容 |
| `ci(` / `di(` / `yi(` | 修改 / 删除 / 复制圆括号内内容 |
| `ca(` / `da(` / `ya(` | 同上，但包含圆括号本身 |
| `cit` / `dat` | 修改 / 删除 HTML 标签内内容 |

在可视模式中：`y` 复制、`d` 删除、`c` 修改、`>` / `<` 缩进；LazyVim 中用 `Alt+j` / `Alt+k` 可向下 / 向上移动所选内容。

## 搜索与替换

| 按键 | 功能 |
| --- | --- |
| `/{文本}` / `?{文本}` | 向后 / 向前搜索 |
| `n` / `N` | 下一个 / 上一个搜索结果 |
| `*` / `#` | 向后 / 向前搜索光标下单词 |
| `:%s/旧/新/g` | 全文替换 |
| `:%s/旧/新/gc` | 全文替换并逐个确认 |
| `:'<,'>s/旧/新/g` | 仅在可视选择范围内替换 |
| `<leader>/` | 在项目根目录全文搜索（Grep） |
| `<leader>sg` / `<leader>sG` | 在项目根目录 / 当前目录全文搜索 |
| `<leader>sw` / `<leader>sW` | 搜索光标单词或选中文本（根目录 / 当前目录） |
| `<leader>sR` | 恢复上一次搜索界面 |

## 文件、项目与 Explorer

| 按键 | 功能 |
| --- | --- |
| `<leader><space>` | 在项目根目录查找文件 |
| `<leader>ff` / `<leader>fF` | 查找文件（项目根目录 / 当前目录） |
| `<leader>fg` | 查找 Git 跟踪的文件 |
| `<leader>fr` / `<leader>fR` | 最近文件（全部 / 当前目录） |
| `<leader>fc` | 查找 Neovim 配置文件 |
| `<leader>fp` | 切换项目 |
| `<leader>e` / `<leader>E` | 打开文件浏览器（项目根目录 / 当前目录） |
| `<leader>fn` | 新建文件 |

在搜索或选择界面中通常可用：`<C-j>` / `<C-k>` 移动，`Enter` 打开，`Esc` 关闭；界面底部或 `?` 可查看该选择器的局部帮助。

## Buffer、窗口与标签页

### Buffer

| 按键 | 功能 |
| --- | --- |
| `Shift+h` / `Shift+l` | 上一个 / 下一个 Buffer |
| `[b` / `]b` | 上一个 / 下一个 Buffer |
| `<leader>,` | 列出并切换 Buffer |
| `<leader>bb` | 切换到另一个 Buffer |
| `<leader>bd` | 删除当前 Buffer |
| `<leader>bD` | 删除当前 Buffer 和窗口 |
| `<leader>bo` | 删除其他 Buffer |
| `<leader>bp` | 固定 / 取消固定 Buffer |

### 窗口

| 按键 | 功能 |
| --- | --- |
| `<leader>-` | 下方水平分屏 |
| `<leader>\|` | 右侧垂直分屏（实际按 `Space` 后按 `|`） |
| `<C-h/j/k/l>` | 移动到左 / 下 / 上 / 右窗口 |
| `<C-Up/Down>` | 增加 / 减少窗口高度 |
| `<C-Left/Right>` | 减少 / 增加窗口宽度 |
| `<leader>wd` | 关闭当前窗口 |
| `<leader>wm` | 切换当前窗口最大化 |
| `<C-w><space>` | 打开窗口操作提示模式 |

### 标签页

| 按键 | 功能 |
| --- | --- |
| `<leader><tab><tab>` | 新建标签页 |
| `<leader><tab>]` / `<leader><tab>[` | 下一个 / 上一个标签页 |
| `<leader><tab>d` | 关闭标签页 |
| `<leader><tab>o` | 关闭其他标签页 |
| `<leader><tab>l` | 返回上一个标签页 |

## 代码、LSP 与诊断

这些键位通常要在对应语言的 LSP 正常启动后才生效。

| 按键 | 功能 |
| --- | --- |
| `gd` | 跳转到定义 |
| `gD` | 跳转到声明 |
| `gr` | 查找引用 |
| `gI` | 跳转到实现 |
| `gy` | 跳转到类型定义 |
| `K` | 显示悬浮文档 |
| `gK` | 显示函数签名帮助 |
| `<C-k>` | 插入模式下显示签名帮助 |
| `<leader>ca` | Code Action / 快速修复 |
| `<leader>cr` | 重命名符号 |
| `<leader>cR` | 重命名文件 |
| `<leader>cf` | 格式化文件或选中内容 |
| `<leader>cl` | 查看 LSP 信息 |
| `<leader>ss` / `<leader>sS` | 文件符号 / 工作区符号 |
| `<leader>cd` | 显示当前行诊断 |
| `]d` / `[d` | 下一个 / 上一个诊断 |
| `]e` / `[e` | 下一个 / 上一个错误 |
| `]w` / `[w` | 下一个 / 上一个警告 |
| `<leader>xx` | 打开项目诊断列表（Trouble） |
| `<leader>xX` | 打开当前 Buffer 诊断列表 |
| `<leader>cs` | 打开代码符号列表 |

## 注释

| 按键 | 功能 |
| --- | --- |
| `gcc` | 切换当前行注释 |
| `gc{范围}` | 注释指定范围，如 `gcap` 注释当前段落 |
| 可视选择后 `gc` | 切换所选内容注释 |
| `gco` / `gcO` | 在下方 / 上方添加一行注释 |

## Git

| 按键 | 功能 |
| --- | --- |
| `<leader>gs` | Git 状态 |
| `<leader>gd` | 查看 Git diff hunks |
| `<leader>gD` | 与 origin 比较 |
| `<leader>gb` | 显示当前行 blame |
| `<leader>gl` / `<leader>gL` | Git 日志（当前目录 / 项目根目录） |
| `<leader>gf` | 当前文件历史 |
| `<leader>gB` | 在浏览器打开仓库对应位置 |
| `<leader>gY` | 复制仓库对应位置的 URL |
| `]h` / `[h` | 下一个 / 上一个 Git hunk（若相关插件已启用） |

## 终端

| 按键 | 功能 |
| --- | --- |
| `<C-/>` | 打开 / 隐藏项目根目录终端 |
| `<leader>ft` / `<leader>fT` | 终端（项目根目录 / 当前目录） |
| `<C-/>` | 在终端模式中隐藏终端 |
| `<C-\><C-n>` | 从终端模式回到普通模式（Neovim 原生） |

## 实用开关与管理

| 按键 / 命令 | 功能 |
| --- | --- |
| `<leader>uf` / `<leader>uF` | 切换全局 / 当前 Buffer 自动格式化 |
| `<leader>uw` | 切换自动换行 |
| `<leader>ul` | 切换行号 |
| `<leader>uL` | 切换相对行号 |
| `<leader>ud` | 切换诊断显示 |
| `<leader>us` | 切换拼写检查 |
| `<leader>uh` | 切换 Inlay Hints |
| `<leader>ug` | 切换缩进辅助线 |
| `<leader>uz` | 切换 Zen 模式 |
| `<leader>uZ` | 切换窗口缩放模式 |
| `<leader>uC` | 选择配色方案 |
| `<leader>l` | 打开插件管理器 Lazy |
| `:LazyExtras` | 管理 LazyVim Extras |
| `:Mason` | 管理 LSP、格式化器、调试器等工具 |
| `:checkhealth` | 检查 Neovim 与插件环境 |

## 最值得先形成肌肉记忆的组合

1. `Space`：打开快捷键提示。
2. `<leader><space>`：找文件；`<leader>/`：搜项目文本。
3. `<leader>e`：文件树；`<leader>,`：切换 Buffer。
4. `gd` / `gr` / `K`：定义、引用、文档。
5. `<leader>ca` / `<leader>cr` / `<leader>cf`：修复、重命名、格式化。
6. `ciw`、`ci"`、`ci(`：快速修改单词、引号、括号内内容。
7. `gcc`：注释；`u` / `<C-r>`：撤销 / 重做。
8. `<C-h/j/k/l>`：在分屏之间移动。
9. `<C-/>`：打开或隐藏终端。
10. `<leader>sk`：忘记按键时搜索快捷键。

## 查看当前配置中的真实映射

速查表可能因插件和个人配置而与本机略有差异，以下方法最可靠：

```vim
:map                 " 所有普通、可视、选择、操作符等待模式映射
:nmap                " 普通模式映射
:imap                " 插入模式映射
:verbose nmap gd     " 查看 gd 的映射及最后定义位置
:Telescope keymaps   " 使用 Telescope Extra 时搜索键位
```

LazyVim 内也可直接用 `<leader>sk` 搜索当前实际生效的 Keymaps。

## 参考

- [LazyVim 官方 Keymaps](https://www.lazyvim.org/keymaps)
- [LazyVim Keymaps 配置说明](https://www.lazyvim.org/configuration/keymaps)
- Neovim 内置帮助：`:help motion.txt`、`:help operator`、`:help text-objects`
